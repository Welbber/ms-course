spring:
  jpa.open-in-view: true
  application:
    name: hr-worker
    datasource:
      url: jdbc:h2:mem:testdb
      username: sa
      password:
  h2:
    console:
      enabled: true
      path: /h2-console
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

server.port: 8001